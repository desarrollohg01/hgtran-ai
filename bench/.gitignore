# Generated by runs, machine-specific — never committed.
hgtran-ai-bench
results-*.json
comparison.json
session*.jsonl
session*.jsonl.shim/

# Build output: the package directory name, produced by `go build ./...`.
bench
